[project]
name = "ag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "agentscope[full]>=2.0.0",
]

[tool.uv.workspace]
members = [
    "ag",
]
